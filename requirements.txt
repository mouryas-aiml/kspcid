# Deployment contract: Synthetic Bengaluru dataset (blr-synthetic-v1.0.0).
# Changing this manifest forces Streamlit Cloud to rebuild the Python process
# after the dataset migration instead of retaining pre-migration modules.
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.17.0
folium>=0.14.0
streamlit-folium>=0.15.0
scikit-learn>=1.3.0
